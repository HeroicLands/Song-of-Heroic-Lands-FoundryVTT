---
"sohl": patch
---

**KB: pick up the nav dropdown fix + API/KB links (#442)**

Bumps the shared theme submodule (`kb/themes/`) to the commit that fixes the
Projects dropdown hover gap and adds the **API Documentation** and
**KnowledgeBase** links, so the knowledgebase's masthead dropdown stays open long
enough to click an item and cross-links the three properties.
