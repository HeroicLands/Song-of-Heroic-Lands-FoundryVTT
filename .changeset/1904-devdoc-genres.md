---
"sohl": patch
---

**Every developer documentation page declares its genre.** Each page under the
knowledgebase's developer section carries a `reference`, `howto` or `concept`
subtype matching the section it lives in, so the site groups it, breadcrumbs it
and lists it beside its siblings the same way every other genre-bearing page
already is.
