{{!--
This file is part of the Song of Heroic Lands (SoHL) system for Foundry VTT.
Copyright (c) 2024–2026 Tom Rodriguez ("Toasty") <toasty@heroiclands.org>

Licensed under the GNU General Public License v3.0 (GPLv3).
You may copy, modify, and distribute this file under the terms of that license.

See LICENSE.md in the project root or:
https://www.gnu.org/licenses/gpl-3.0.html

SPDX-License-Identifier: GPL-3.0-or-later
--}}

<div class="sohl chat-card item-card" data-actor-id="{{actorId}}">
    <header class="card-header flexrow">
        <h3 class="title">{{title}}</h3>
    </header>

    {{#if notes}}
    <div class="card-note">
        <span>{{notes}}</span>
    </div>
    {{/if}}

    <div class="grid grid-2col">
        <div class="card-content center">
            <div class="flexrow">
                <span class="label">{{localize "SOHL.Chat.Damage.formula"}}:</span>
                <span class="value">{{impactLabel}}</span>
            </div>
        </div>

        <div class="card-content center">
            <div class="flexrow">
                <span class="label">{{localize "SOHL.Chat.Damage.roll"}}:</span>
                <span class="value"
                    >{{rollResult}} = <strong>{{impact}}</strong></span
                >
            </div>
        </div>
    </div>

    <div class="card-content center">
        <div class="flexrow">
            <span class="label">{{localize "SOHL.ImpactModifier.Aspect.label"}}:</span>
            <span class="value">{{#if aspect}}{{localize (concat "SOHL.ImpactModifier.Aspect." aspect)}}{{/if}}</span>
        </div>
    </div>

    {{#if hasTarget}}
    <div class="card-buttons">
        <button
            data-action="resolveInjury"
            data-handler-uuid="{{handlerUuid}}"
            data-source-actor-uuid="{{sourceActorUuid}}"
            data-scope="{{toJSON scopeData}}">
            {{localize "SOHL.Chat.Damage.calculateInjury" target=targetName}}
        </button>
    </div>
    {{/if}}
</div>
