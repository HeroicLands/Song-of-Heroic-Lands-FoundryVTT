describe("ImpactResult", () => {
    describe("constructor", () => {
        it.todo("creates instance extending SuccessTestResult with default values");
        it.todo("initializes impactModifier from data or creates default ImpactModifier");
        it.todo("initializes deliversImpact to false by default");
        it.todo("throws when no parent is provided");
    });

    describe("properties", () => {
        it.todo("impactModifier is accessible");
        it.todo("deliversImpact is accessible and settable");
    });

    describe("inheritance", () => {
        it.todo("inherits all SuccessTestResult properties (successLevel, roll, etc.)");
        it.todo("inherits evaluate behavior from SuccessTestResult");
        it.todo("inherits toChat behavior from SuccessTestResult");
    });
});
