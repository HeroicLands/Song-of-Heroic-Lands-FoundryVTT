**/audio
**/css
**/fonts
**/images
**/lang
**/nogit
**/packs
**/system.json
**/package-lock.json
**/*.hbs

# Auto-generated by `npm run docs`; its formatting is irrelevant and it is not
# hand-edited, so keep Prettier from flagging its regenerated output.
docs/reference/type-catalog.md
assets/content/User_Guide/Icon_Legend.md

# Regenerated by `npx changeset version` on every release (see the Release
# workflow), assembled from the `.changeset/*.md` bodies — not hand-edited.
# Prettier's output would re-drift on the next release, and because changesets
# indents each wrapped body into a list item, reformatting reflows continuation
# lines out of their bullet rather than tidying them. Leave it unmanaged.
CHANGELOG.md

# Auto-generated by `npm run build:icons` (utils/build-icon-font.mjs); a compact
# one-rule-per-line glyph map that is not hand-edited. Prettier would explode it
# ~3x and it would re-drift on every regeneration, so leave it unmanaged.
scss/abstracts/_icons.scss


packages/sohl-types/index.d.ts

# The knowledgebase is a self-contained Astro sub-project with its own tooling
# (the root Prettier has no .astro parser); it manages its own formatting.
kb/
